# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
python-multipart==0.0.20

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
pgvector==0.3.6
greenlet==3.1.1

# --- Cache / queue ---
redis==5.2.1
celery==5.4.0

# --- Rate limiting ---
slowapi==0.1.9

# --- Parsing ---
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
pdfplumber==0.11.4
python-docx==1.1.2
openpyxl==3.1.5

# --- Normalization / search ---
rapidfuzz==3.11.0
# NOTE: semantic search deps (sentence-transformers + torch) live in
# requirements-semantic.txt — install them and set ENABLE_SEMANTIC=true to turn it on.
# Lexical (pg_trgm) search always works; hybrid degrades to lexical without them.

# --- Misc ---
python-dotenv==1.0.1
tenacity==9.0.0
